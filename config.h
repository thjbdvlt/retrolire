// configuration for retrolire.

// the command used to open files and urls.
static const char opener[] = "xdg-open";

// the editor.
static const char* editor = "$EDITOR";

// the default subcommand, when no subcommand is used.
static char default_command[] = "edit";

// string used te the connection to database.
static const char connectioninfo[] = "dbname=retrolire";

// preview window position (fzf).
static char preview_pos[] = "right,45%";

static char isbn_services[] = "openl wiki goob";
